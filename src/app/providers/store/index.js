import {createStore} from 'vuex'
import * as modules from '../../models'

export const store = createStore({
    state: {},
    getters: {},
    mutations: {},
    actions: {},
    modules: modules,
})
